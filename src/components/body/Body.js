import React from 'react';

export default function Body() {
    return (
        <div className="body">
            <button> Cats </button>
            <button> Dogs </button>
            <button> Reset </button>
        </div>
    );
}